# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Audio Processing (FFmpeg-free stack) ───────────────────────────────────────
# soundfile is the primary decoder (WAV/FLAC/OGG/AIFF — no external binaries)
# pydub kept in case of transitive imports but is NO LONGER called by our code
pydub==0.25.1
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4
scipy==1.13.0

# ── Machine Learning ───────────────────────────────────────────────────────────
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.2

# ── NLP / Sentiment ────────────────────────────────────────────────────────────
nltk==3.8.1
vaderSentiment==3.3.2

# ── Utilities / Sarvam AI HTTP Client ─────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.0          # used by sarvam_service.py for TTS/STT API calls
